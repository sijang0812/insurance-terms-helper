# llm-gateway-service 설정
server:
  port: 8083

spring:
  application:
    name: llm-gateway-service

# [다음 단계에서 채울 예정]
# Anthropic / OpenAI API 키는 절대 코드나 git에 직접 커밋하지 않는다.
# 환경변수(ANTHROPIC_API_KEY, OPENAI_API_KEY)로 주입받도록 설계해둔다.
# llm:
#   claude:
#     api-key: ${ANTHROPIC_API_KEY}
#   openai:
#     api-key: ${OPENAI_API_KEY}
