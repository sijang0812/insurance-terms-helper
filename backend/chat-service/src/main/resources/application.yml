# chat-service 설정
server:
  port: 8082

spring:
  application:
    name: chat-service

# 호출할 서비스 URL을 직접 명시한다 (Eureka 등 디스커버리 없이 가는 1차 구성).
# 서비스가 늘어나면 이 부분을 서비스 디스커버리로 교체하는 것을 검토한다.
services:
  document-service:
    url: http://localhost:8081
  llm-gateway-service:
    url: http://localhost:8083
