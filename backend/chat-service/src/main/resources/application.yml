# chat-service 설정
server:
  port: ${PORT:8082}

spring:
  application:
    name: chat-service

# 호출할 서비스 URL. 운영 환경에서는 환경변수로 주입한다.
services:
  document-service:
    url: ${DOCUMENT_SERVICE_URL:http://localhost:8081}
  llm-gateway-service:
    url: ${LLM_GATEWAY_URL:http://localhost:8083}
